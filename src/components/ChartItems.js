import React, { useState } from 'react';
import { useGetTopChartsQuery } from '../redux/services/shazamCore';

function ChartPage() {
  const [data, setChartItems] = useState([]);

}
 export default ChartPage ;